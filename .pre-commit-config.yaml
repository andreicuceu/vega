repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Always check for the latest version by running `pre-commit autoupdate`
    rev: v0.16.7
    hooks:
      # Run the Ruff linter
      - id: ruff
        args: [ --fix ]
      # Run the Ruff formatter
      - id: ruff-format
